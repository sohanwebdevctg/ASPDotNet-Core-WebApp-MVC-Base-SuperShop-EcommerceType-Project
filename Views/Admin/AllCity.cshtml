@model IEnumerable<SuperShop.Models.City>;
@{
    ViewData["Title"] = "AllCity";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<!-- all city start -->
<div class="table-content-area">
    <div class="myContainer">
        @if (TempData["Error"] != null)
        {
            <div class="alert" style="color: red; font-weight: bold; margin-bottom: 10px;">
                @TempData["Error"]
            </div>
        }

        @if (TempData["Success"] != null)
        {
            <div class="alert" style="color: green; font-weight: bold; margin-bottom: 10px;">
                @TempData["Success"]
            </div>
        }
        <h3 class="table-title">Total : @Model.Count()</h3>
        <div class="table-wrapper">
            <div class="table-container">
                <table class="table-content">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>CityName</th>
                            <th>TotlaUser</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>

                        @if (Model.Count() > 0)
                        {
                            int serialNo = 1;
                            @foreach (var data in Model)
                            {
                                <tr>
                                    <td>@(serialNo++)</td>
                                    <td>@data.CityName</td>
                                    <td>@(data.Users != null ? data.Users.Count() : 0)</td>
                                    <td class="btn-wrapper">
                                        <a href="@Url.Action("EditCity", "Admin", new {id = data.CityId})" class="table-btn" style="background: linear-gradient(135deg, #FFEDBC 0%, #28a745 100%);">Edit</a>
                                        <form asp-controller="Admin" asp-action="DeleteCity" method="post">
                                            <input type="hidden" name="id" value="@data.CityId" />
                                            <button type="submit" href="#" class="table-btn" style="background: linear-gradient(135deg, #FFEDBC 0%, #dc3545 100%);">Delete</button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="4" class="text-danger text-center">@ViewBag.message</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
<!-- all city end -->
