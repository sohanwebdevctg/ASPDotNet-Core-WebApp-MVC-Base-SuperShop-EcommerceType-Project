@model SuperShop.Models.Gender;
@{
    ViewData["Title"] = "CreateGender";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<!-- create gender start -->
<div class="myContainer">
    <div class="common-form-wrapper">
        <form asp-controller="Admin" asp-action="CreateGender" method="post" class="master-form">
            <div>
                <h2 class="form-title">Create Gender</h2>
            </div>
            <div class="form-group">
                <input asp-for="GenderName" type="text" placeholder="Enter Your Gender Name" class="common-input" />
                <span asp-validation-for="GenderName" class="text-danger" style="font-size: 15px;"></span>
            </div>
            <div class="form-group common-submit-wrapper">
                <button type="submit" class="common-submit-btn" style="background: linear-gradient(135deg, #FFEDBC 0%, #ED4264 100%);">SUBMIT DATA</button>
                <a href="@Url.Action("AllGender", "Admin")" class="common-submit-btn" style="background: linear-gradient(135deg, #FFEDBC 0%, #28a745 100%);">ALL GENDER</a>
            </div>
           
        </form>
    </div>
</div>

<!-- create gender end -->

